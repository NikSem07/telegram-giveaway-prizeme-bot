// webapp/pages/creator/services/services.template.js

/**
 * Данные о сервисах — единый источник истины.
 * При добавлении нового сервиса достаточно добавить объект в этот массив.
 */
const SERVICES = [
    {
        id:          'top_placement',
        emoji:       '🏆',
        title:       'Включение в Топ-розыгрыши',
        description: 'Ваш розыгрыш будет показан в блоке «Топ» на главной странице. Каждый участник увидит его сразу при открытии приложения.',
    },
    {
        id:          'bot_promotion',
        emoji:       '📣',
        title:       'Продвижение розыгрыша в боте',
        description: 'Ваш розыгрыш будет разослан всей аудитории бота. Каждый пользователь получит уведомление с возможностью сразу принять участие.',
    },
    {
        id:          'tasks',
        emoji:       '✅',
        title:       'Задания для участников',
        description: 'Создайте задания для участников вашего розыгрыша. За выполнение они получают дополнительные билеты, что повышает вовлечённость.',
    },
];

export default function servicesTemplate(context = {}) {
    const serviceCards = SERVICES.map(s => `
        <div class="svc-card" data-service-id="${s.id}" role="button" tabindex="0" aria-pressed="false">
            <div class="svc-card-header">
                <span class="svc-card-emoji">${s.emoji}</span>
                <span class="svc-card-title">${s.title}</span>
            </div>
            <p class="svc-card-desc">${s.description}</p>
        </div>
    `).join('');

    return `
        <div class="svc-screen">

            <!-- Анимация и заголовок раздела -->
            <div class="svc-hero">
                <div class="svc-hero-anim" id="svc-hero-anim"></div>
                <h1 class="svc-hero-title">Сервисы для создателей</h1>
                <p class="svc-hero-subtitle">Выберите один из сервисов ниже для вовлечения своей аудитории</p>
            </div>

            <!-- Список сервисов -->
            <div class="svc-list">
                ${serviceCards}
            </div>

            <!-- Отступ снизу — чтобы контент не уходил под кнопку "Продолжить" -->
            <div class="svc-bottom-spacer"></div>
        </div>

        <!-- Кнопка "Продолжить" — фиксированная, появляется только при выборе -->
        <div class="svc-footer" id="svc-footer" aria-hidden="true">
            <button class="svc-continue-btn" id="svc-continue-btn" type="button">
                Продолжить
            </button>
        </div>
    `;
}
